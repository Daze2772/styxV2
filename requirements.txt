patchright>=1.49.0
opencv-python>=4.9.0
numpy>=1.26.0
loguru>=0.7.2
# Optional second-engine for the hardest Cloudflare cases.
# Install separately:
#   pip install "camoufox[geoip]"
#   camoufox fetch
